services:
  db:
    container_name: ncms-db
    image: postgres:alpine
    env_file:
      - .env.local
    ports:
      - 5432:5432
    volumes:
      - data:/var/lib/postgresql/data
volumes:
  data: